"""Shared kernel types used across bounded contexts.

All model classes have been moved to domain-specific subpackages under `models/`.
"""

type DomainPrimaryKeyType = int
type EmployeeIdType = int
type FacilityIdType = int
type FacilityIdKey = int
type ShiftIdKey = int
